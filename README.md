# SNU 반려동물 검진센터 시스템

서울대학교 반려동물 검진센터를 위한 종합 건강 관리 시스템입니다. 환자(반려동물) 정보 관리, 검사 결과 추적, 방문 기록, 문진 관리 등의 기능을 제공합니다.

## 📋 목차

- [프로젝트 개요](#프로젝트-개요)
- [주요 기능](#주요-기능)
- [기술 스택](#기술-스택)
- [프로젝트 구조](#프로젝트-구조)
- [시작하기](#시작하기)
- [개발 가이드](#개발-가이드)
- [API 문서](#api-문서)
- [아키텍처](#아키텍처)

## 프로젝트 개요

SNU 반려동물 검진센터 시스템은 반려동물의 건강 데이터를 체계적으로 관리하고 분석하기 위한 웹 애플리케이션입니다. 

### 주요 목표

- 📊 **데이터 중심 관리**: 검사 결과, 방문 기록, 문진 응답 등을 체계적으로 저장 및 관리
- 🔍 **고급 필터링**: 품종, 나이, 검사 결과 등 다양한 조건으로 환자 필터링
- 📈 **시각화**: 건강 데이터를 차트와 그래프로 직관적으로 표현
- 💊 **약물 관리**: 처방 약물 및 투여 기록 추적
- 📁 **Excel 연동**: 데이터 업로드 및 다운로드 지원

## 주요 기능

### 환자 관리
- 환자(반려동물) 등록 및 정보 관리
- 동물번호, 품종, 나이, 성별, 중성화 여부 등 상세 정보 저장
- 환자별 통계 및 건강 이력 조회

### 검사 결과 관리
- CBC, 간기능, 신장기능, 전해질 등 다양한 검사 항목 지원
- 정량적/정성적 검사 결과 입력
- 정상 범위 대비 상태 표시 (High/Normal/Low)
- 검사 결과 시각화 (차트, 레이더 그래프)

### 방문 기록
- 방문 일자, 유형, 주증상, 진단, 처치 내역 기록
- 방문별 검사 결과 및 약물 처방 연결

### 문진 관리
- 카테고리별 문진 템플릿 (피부, 소화기, 호흡기, 신경, 비뇨기, 근골격)
- 문진 응답 기록 및 이력 조회

### 필터 및 군집 분석
- 사용자 정의 필터 생성 및 저장
- 품종, 나이, 검사 결과 기반 군집 분석
- 저장된 필터 재사용

### 데이터 관리
- Excel 파일 업로드를 통한 대량 데이터 입력
- 현재 데이터 Excel 다운로드

## 기술 스택

### Frontend
- **React 18** - UI 라이브러리
- **TypeScript** - 타입 안전성
- **Vite** - 빌드 도구
- **Wouter** - 경량 라우팅
- **TanStack Query (React Query)** - 서버 상태 관리
- **Zustand** - 클라이언트 상태 관리
- **Tailwind CSS** - 스타일링
- **Radix UI** - 접근성 높은 UI 컴포넌트
- **Recharts** - 데이터 시각화
- **React Three Fiber** - 3D 모델 렌더링

### Backend
- **NestJS** - Node.js 프레임워크
- **Express** - HTTP 서버
- **TypeScript** - 타입 안전성
- **Drizzle ORM** - 데이터베이스 ORM
- **PostgreSQL** - 데이터베이스 (선택적)
- **Zod** - 스키마 검증

### Development Tools
- **ESBuild** - 빠른 번들링
- **Playwright** - E2E 테스트
- **Drizzle Kit** - 데이터베이스 마이그레이션

## 프로젝트 구조

```
SNUClinicSystem/
├── client/                      # 프론트엔드 애플리케이션
│   ├── src/
│   │   ├── components/         # React 컴포넌트
│   │   │   ├── ui/            # 재사용 가능한 UI 컴포넌트
│   │   │   ├── charts/        # 차트 컴포넌트
│   │   │   ├── app-sidebar.tsx
│   │   │   ├── dog-model-3d.tsx
│   │   │   └── stats-card.tsx
│   │   ├── pages/             # 페이지 컴포넌트
│   │   │   ├── home.tsx       # 대시보드
│   │   │   ├── patients.tsx   # 환자 목록
│   │   │   ├── patient-detail.tsx
│   │   │   ├── visits.tsx
│   │   │   ├── test-results.tsx
│   │   │   ├── questionnaires.tsx
│   │   │   ├── filters.tsx
│   │   │   └── data-management.tsx
│   │   ├── lib/               # 유틸리티 및 설정
│   │   │   ├── api.ts        # API 클라이언트
│   │   │   ├── queryClient.ts # React Query 설정
│   │   │   ├── store.ts      # Zustand 스토어
│   │   │   └── utils.ts      # 헬퍼 함수
│   │   ├── types/            # TypeScript 타입 정의
│   │   ├── hooks/            # 커스텀 React 훅
│   │   ├── App.tsx           # 루트 컴포넌트
│   │   ├── main.tsx          # 엔트리 포인트
│   │   └── index.css         # 글로벌 스타일
│   └── index.html
├── server/                      # 백엔드 애플리케이션
│   ├── controllers/            # NestJS 컨트롤러
│   │   ├── patients.controller.ts
│   │   ├── visits.controller.ts
│   │   ├── test-results.controller.ts
│   │   ├── questionnaire.controller.ts
│   │   ├── filters.controller.ts
│   │   ├── medications.controller.ts
│   │   └── excel.controller.ts
│   ├── common/                 # 공통 모듈
│   │   ├── filters/           # 예외 필터
│   │   └── interceptors/      # 인터셉터
│   ├── config/                 # 설정 관리
│   │   └── index.ts
│   ├── utils/                  # 유틸리티
│   │   └── excelReader.ts
│   ├── app.module.ts           # 루트 모듈
│   ├── main.ts                 # 서버 엔트리 포인트
│   ├── storage.ts              # 인메모리 스토리지
│   ├── storage.service.ts      # 스토리지 서비스
│   └── vite.ts                 # Vite 개발 서버 설정
├── shared/                      # 공유 코드
│   └── schema.ts               # 데이터베이스 스키마 및 타입
├── tests/                       # E2E 테스트
├── attached_assets/             # 첨부 파일
├── package.json
├── tsconfig.json
├── vite.config.ts
├── drizzle.config.ts
├── playwright.config.ts
└── README.md
```

## 시작하기

### 사전 요구사항

- **Node.js** 20.x 이상
- **npm** 또는 **yarn**

### 설치

1. **저장소 클론**

```bash
git clone <repository-url>
cd SNUClinicSystem
```

2. **의존성 설치**

```bash
npm install
```

3. **환경 변수 설정** (선택사항)

`.env.example` 파일을 참고하여 `.env` 파일을 생성합니다:

```bash
cp .env.example .env
```

기본 설정:
```env
PORT=5000
NODE_ENV=development
```

### 개발 서버 실행

```bash
npm run dev
```

서버가 시작되면 브라우저에서 http://localhost:5000 으로 접속합니다.

### 프로덕션 빌드

```bash
npm run build
npm start
```

## 개발 가이드

### 타입 체크

```bash
npm run check
```

### E2E 테스트

```bash
# 테스트 실행
npm run test:e2e

# UI 모드로 테스트
npm run test:e2e:ui
```

### 데이터베이스 마이그레이션

현재는 인메모리 스토리지를 사용하지만, PostgreSQL로 전환 시:

```bash
# 스키마 푸시
npm run db:push
```

### 코드 구조 가이드라인

#### API 클라이언트 사용

`client/src/lib/api.ts`의 중앙화된 API 클라이언트를 사용하세요:

```typescript
import { api } from '@/lib/api';

// 환자 목록 조회
const patients = await api.patients.getAll();

// 환자 생성
const newPatient = await api.patients.create({
  animalNumber: 'A001',
  name: '뽀삐',
  // ...
});
```

#### React Query 사용

`queryKeys`를 사용하여 일관된 쿼리 키를 생성하세요:

```typescript
import { useQuery } from '@tanstack/react-query';
import { api } from '@/lib/api';
import { queryKeys } from '@/lib/queryClient';

function PatientList() {
  const { data, isLoading } = useQuery({
    queryKey: queryKeys.patients.all,
    queryFn: () => api.patients.getAll(),
  });
  
  // ...
}
```

#### 컨트롤러 작성

NestJS 컨트롤러는 다음 패턴을 따릅니다:

```typescript
import { Controller, Get, Post, Body } from '@nestjs/common';
import { StorageService } from '../storage.service';

@Controller('resource')
export class ResourceController {
  constructor(private readonly storage: StorageService) {}
  
  @Get()
  async getAll() {
    return this.storage.getAllResources();
  }
  
  @Post()
  async create(@Body() data: CreateDto) {
    return this.storage.createResource(data);
  }
}
```

## API 문서

### Base URL

개발: `http://localhost:5000/api`

### 엔드포인트

#### Patients (환자)

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/patients` | 모든 환자 조회 |
| GET | `/patients/:animalNumber` | 특정 환자 조회 |
| POST | `/patients` | 환자 생성 |
| PUT | `/patients/:id` | 환자 정보 수정 |
| DELETE | `/patients/:id` | 환자 삭제 |

#### Visits (방문)

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/visits` | 모든 방문 조회 |
| GET | `/visits/:id` | 특정 방문 조회 |
| GET | `/visits/animal/:animalNumber` | 특정 환자의 방문 기록 |
| POST | `/visits` | 방문 생성 |
| PUT | `/visits/:id` | 방문 정보 수정 |
| DELETE | `/visits/:id` | 방문 삭제 |

#### Test Results (검사 결과)

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/test-results` | 모든 검사 결과 조회 |
| GET | `/test-results/:id` | 특정 검사 결과 조회 |
| GET | `/test-results/animal/:animalNumber` | 특정 환자의 검사 결과 |
| GET | `/test-results/exam-master` | 검사 항목 마스터 조회 |
| POST | `/test-results` | 검사 결과 생성 |
| PUT | `/test-results/:id` | 검사 결과 수정 |
| DELETE | `/test-results/:id` | 검사 결과 삭제 |

#### Questionnaire (문진)

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/questionnaire/templates` | 문진 템플릿 조회 |
| GET | `/questionnaire/responses` | 모든 문진 응답 조회 |
| GET | `/questionnaire/responses/animal/:animalNumber` | 특정 환자의 문진 응답 |
| POST | `/questionnaire/responses` | 문진 응답 생성 |

#### Filters (필터)

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/filters` | 모든 필터 조회 |
| POST | `/filters` | 필터 생성 |
| DELETE | `/filters/:id` | 필터 삭제 |
| GET | `/filters/clusters` | 군집 분석 조회 |
| POST | `/filters/clusters` | 군집 생성 |
| DELETE | `/filters/clusters/:id` | 군집 삭제 |

#### Medications (약물)

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/medications` | 모든 약물 조회 |
| GET | `/medications/animal/:animalNumber` | 특정 환자의 약물 |
| POST | `/medications` | 약물 생성 |
| PUT | `/medications/:id` | 약물 수정 |
| DELETE | `/medications/:id` | 약물 삭제 |

#### Excel (데이터 관리)

| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/excel/upload` | Excel 파일 업로드 |
| GET | `/excel/download` | 데이터 Excel 다운로드 |

### 요청/응답 예시

#### 환자 생성

**Request:**
```http
POST /api/patients
Content-Type: application/json

{
  "animalNumber": "A001",
  "name": "뽀삐",
  "ownerName": "김철수",
  "species": "개",
  "breed": "푸들",
  "gender": "수컷",
  "birthDate": "2020-01-15",
  "neutered": true,
  "weight": 5.5
}
```

**Response:**
```json
{
  "id": "uuid-here",
  "animalNumber": "A001",
  "name": "뽀삐",
  "ownerName": "김철수",
  "species": "개",
  "breed": "푸들",
  "gender": "수컷",
  "birthDate": "2020-01-15",
  "registrationDate": "2025-11-20",
  "neutered": true,
  "weight": 5.5,
  "microchipNumber": null
}
```

## 아키텍처

### 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                      Client (Browser)                    │
│  ┌──────────────────────────────────────────────────┐  │
│  │  React + TypeScript + Tailwind CSS               │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐ │  │
│  │  │   Pages    │  │ Components │  │   Hooks    │ │  │
│  │  └────────────┘  └────────────┘  └────────────┘ │  │
│  │  ┌────────────────────────────────────────────┐ │  │
│  │  │  TanStack Query (Server State)            │ │  │
│  │  └────────────────────────────────────────────┘ │  │
│  │  ┌────────────────────────────────────────────┐ │  │
│  │  │  API Client (Type-safe)                   │ │  │
│  │  └────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                          │
                          │ HTTP/REST
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    Server (Node.js)                      │
│  ┌──────────────────────────────────────────────────┐  │
│  │  NestJS + Express                                │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐ │  │
│  │  │Controllers │  │  Services  │  │   Config   │ │  │
│  │  └────────────┘  └────────────┘  └────────────┘ │  │
│  │  ┌────────────────────────────────────────────┐ │  │
│  │  │  Global Filters & Interceptors            │ │  │
│  │  └────────────────────────────────────────────┘ │  │
│  │  ┌────────────────────────────────────────────┐ │  │
│  │  │  Storage Service (In-Memory / DB)         │ │  │
│  │  └────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                          │
                          │ Drizzle ORM
                          ▼
┌─────────────────────────────────────────────────────────┐
│              Database (PostgreSQL / In-Memory)           │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Tables: patients, visits, test_results,        │  │
│  │  exam_master, medications, questionnaires, etc.  │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 데이터 흐름

1. **사용자 요청**: 사용자가 UI에서 액션 수행
2. **React Query**: 캐시 확인 후 필요시 API 호출
3. **API Client**: 타입 안전한 HTTP 요청 생성
4. **NestJS Controller**: 요청 수신 및 검증
5. **Storage Service**: 비즈니스 로직 처리 및 데이터 접근
6. **Database/Memory**: 데이터 저장/조회
7. **응답 반환**: 데이터가 클라이언트로 전달
8. **UI 업데이트**: React가 UI를 자동으로 업데이트

### 주요 설계 패턴

- **Repository Pattern**: StorageService가 데이터 접근 추상화
- **Dependency Injection**: NestJS의 DI 컨테이너 활용
- **Type Safety**: 전체 스택에서 TypeScript 타입 공유
- **Error Handling**: 전역 예외 필터로 일관된 에러 응답
- **Caching**: React Query로 서버 상태 캐싱 및 동기화

## 라이선스

MIT

## 기여

이슈 및 풀 리퀘스트를 환영합니다!

---

**SNU 반려동물 검진센터 시스템** - 반려동물의 건강한 삶을 위한 데이터 관리 솔루션
